/**
 * 定义 数值型 枚举
 */
enum Season {
  Spring,
  Summer,
  Fall,
  Winter,
}

/**
 * 定义字符串型枚举
 */

enum Direction {
  Up = 'up',
  Down = 'down',
  Left = 'left',
  Right = 'right',
}

const enum ThreeNumber {
  One,
  Two,
  Three,
}

const directionItem: string = Direction.Up;

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const point: Object = { x: 12, y: 24 };
const a = (): object => {
  return {};
};
